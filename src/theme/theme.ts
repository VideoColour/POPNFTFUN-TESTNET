import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Bebas Neue', sans-serif`,  // For headings
    body: `'Bebas Neue', sans-serif`,  // For body text
  },
});

export default theme;
